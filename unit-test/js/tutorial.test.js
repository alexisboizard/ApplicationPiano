/* Tests tutorial.js */

function assert(condition, message)
{
    if (!condition) {
        throw new Error(message);
      }
}

function testNext()
{
    assert(/* condition, testNext doit retourner... */);
}

function testPrev()
{
    assert(/* condtion, testPrev doit retourner... */);
}

function testClosetuto()
{
    assert(/* condtion, testCloseTuto doit retourner... */);
}